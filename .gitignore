# Build artifacts
target/
*.o
*.a
*.dylib

.next/

# Rust
# Keep the sidecar lockfile committed for reproducible Rust tests and builds.
Cargo.lock
!coordinator/promptsidecar/Cargo.lock

# Go
coordinator/bin/

# Swift (all .build and .swiftpm dirs anywhere)
.build/
.swiftpm/

# Node.js / TypeScript
node_modules/
dist/
*.tsbuildinfo

# Python
__pycache__/
*.egg-info/
build/
.venv/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# Config (local dev)
*.local.toml
.env
.env.*
coordinator/.env

# Keys and certificates (never commit)
*.pem
*.csr
*.p12
enclave_key.data

# Database dumps (contain prod data)
*.dump

# Compiled binaries (coordinator)
coordinator/eigeninference-coordinator
coordinator/eigeninference-coordinator-linux
coordinator/coordinator

# LaTeX build artifacts
*.aux
*.log
*.out

# Temp directories
tmp/


d-inf-docs/
.vercel
.claude/
.external/

# Generated prompts
claude-voice-system-prompt.md

# Solana test validator ledger (local testing)
test-ledger/

# Internal docs (contain infra details, not for public repo)
docs/coordinator-deploy-runbook.md
docs/.private/
docs/default-private-inference-plan.md

# Rust cross-language test build artifacts
coordinator/internal/e2e/testdata/decrypt/target/
docs/RUNBOOK.md

# Also match a .build symlink (not just the dir).
.build

# Local performance binaries and raw diagnostic captures
/artifacts/

# Worktree-local development toolchains
/.tools/
/provider-tui/bin/
